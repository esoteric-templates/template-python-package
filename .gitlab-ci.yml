stages:
  - build

variables:
  NIX_PATH: nixpkgs=channel:nixos-25.11

build:
  stage: build
  image: nixos/nix:2.18.1
  script:
    - nix --extra-experimental-features 'nix-command flakes' build
    # - ./result/bin/template --version
    # - ./result/bin/template --help
